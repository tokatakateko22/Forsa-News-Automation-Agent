name: Cron Scheduler Heartbeat Test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Report Trigger Time
        run: |
          echo "GitHub Actions cron scheduler triggered successfully at $(date -u)"
